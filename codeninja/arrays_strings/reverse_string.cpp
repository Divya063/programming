#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define N 100005
#define MOD 1000000007
#define dd double
#define DEBUG(x) cout << '>' << #x << ':' << x << endl;



int main() {
ios_base::sync_with_stdio(false);
cin.tie(NULL);
//ios_base& scientific (ios_base& str);
// input - given string
// You need to update in the given string itself. No need to print or return anything

    // Write your code here
	char input[] = {"The", "horse", "is"};
    /*std::string temp;
    int size = sizeof(input[0])/sizeof(char);
    for(int i =size-1, j =0; i>=0 && j < i; j++, i--){
        temp = input[j];
        input[j] = input[i];
        input[i] = temp;
    }*/
    cout << input << endl;


return 0;
}

